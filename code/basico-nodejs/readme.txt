Para poder ejecutar los ejemplos, se debe realizar lo siguiente:

# Iniciar el proyecto con node.js
npm init
# Instalar express
npm i express
# Instalar body-parser
npm i body-parser
# Instalar nodemon para que se ejecute constamente (opcional)
npm i nodemon
